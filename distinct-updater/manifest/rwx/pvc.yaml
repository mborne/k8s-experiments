apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: distinct-updater-pvc
spec:
  #storageClassName: default
  accessModes:
    - ReadWriteMany
  #volumeMode: Block
  resources:
    requests:
      storage: 1Mi
